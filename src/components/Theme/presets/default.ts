import '../_colors/default.scss'
import '../_size/default.scss'

export const presetDefault = {
  color: {
    primary: 'default',
    accent: 'default',
  },
  control: 'default',
  font: 'default',
  size: 'default',
  space: 'default',
  shadow: 'default',
}
